services:
  - type: web
    name: healthcare-portal
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn healthcare_portal.wsgi:application"
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11.0"
